---
tags:
  - type
  - core-data
title: "Component (Type)"
---

The base shape shared by everything `get_component` / `self` returns.

### .id
Programmatic identifier of this component.

**Returns:** string

Every concrete component adds its own API on top; see [[Components Overview]].
